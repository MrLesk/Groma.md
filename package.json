{
  "name": "groma",
  "private": true,
  "type": "module",
  "engines": {
    "bun": ">=1.3.14",
    "node": ">=20.19.0"
  },
  "scripts": {
    "validate:architecture": "node scripts/validate-architecture.mjs",
    "viewer": "bun run src/viewer/server.mjs",
    "test:viewer:browser": "playwright test",
    "test": "node --test",
    "check": "npm run validate:architecture && npm test"
  },
  "dependencies": {
    "@xyflow/react": "12.11.2",
    "comark": "0.5.1",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@playwright/test": "1.62.0"
  }
}
