{
  "name": "groma",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    "./plugin-sdk": "./src/plugin-sdk/index.ts"
  },
  "packageManager": "bun@1.3.14",
  "scripts": {
    "dev": "bun run src/cli/main.ts",
    "build": "bun run scripts/build.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "format": "prettier --write src scripts tests .github ARCHITECTURE.md DEVELOPMENT.md docs package.json tsconfig.json .prettierrc.json",
    "format:check": "prettier --check src scripts tests .github ARCHITECTURE.md DEVELOPMENT.md docs package.json tsconfig.json .prettierrc.json",
    "check:boundaries": "bun run scripts/check-boundaries.ts",
    "check:targets": "bun run scripts/verify-targets.ts",
    "smoke": "bun run scripts/verify-binary.ts",
    "verify:1a": "bun run build && bun run smoke && bun run tests/iteration-1a/verify.ts",
    "check": "bun run format:check && bun run typecheck && bun run check:boundaries && bun run test && bun run verify:1a"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "prettier": "3.9.5",
    "typescript": "7.0.2"
  },
  "dependencies": {
    "@babel/parser": "8.0.4",
    "yaml": "2.9.0"
  }
}
