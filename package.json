{
  "name": "groma",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "validate:architecture": "node scripts/validate-architecture.mjs",
    "scanner": "node src/scanner-process.mjs",
    "test": "node --test",
    "check": "bun run validate:architecture && bun run test"
  },
  "dependencies": {
    "comark": "0.5.1"
  }
}
