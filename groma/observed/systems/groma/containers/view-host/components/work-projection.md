---
id: work-projection
kind: component
parent: view-host
code:
  - scanner: typescript
    file: src/work-projection.ts
    symbol: projectActiveWork
---

# Work projection

Maps in-progress Backlog tasks onto the elements they reference by exact id, with every assignee, so the terminal map can mark where work is happening without reading Backlog itself.
