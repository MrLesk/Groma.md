---
id: semantic-city
kind: component
parent: core
group: World building
code:
  - scanner: typescript
    file: src/semantic-city.ts
    symbol: promotedEndpoint
---

# Semantic city

The shared geometry of the semantic city: the nearest attachable endpoint for a relationship, orthogonal routes between visible boxes, and label placement, so every renderer draws the same promoted edge.
