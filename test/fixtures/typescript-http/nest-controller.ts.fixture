import { Controller, Get, Post } from '@nestjs/common'

@Controller('speakers')
export class SpeakersController {
  @Get(':id')
  find(): string {
    return 'speaker'
  }

  @Post()
  create(): string {
    return 'created'
  }
}
